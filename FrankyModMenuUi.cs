namespace FrankyModMenu;

using JetAnnotations;
using SonsGameManager;
using SonsSdk;
using SUI;
using TMPro;
using UnityEngine.UI;
using static SUI.SUI;

public class FrankyModMenuUi
{
    /*
    public const string PANEL_NAME = "FrankyModMenuUI";
    private static SContainerOptions _panel;
    private static SScrollContainerOptions _MainPanel;
    private static readonly SUI.BackgroundDefinition MainBg = new SUI.BackgroundDefinition("#fff", SUI.GetBackgroundSprite(EBackground.Sons), Image.Type.Simple);
    */
    public static void Create()
    
    {
        //FrankyModMenuUi._panel = RegisterNewPanel(PANEL_NAME, true);
        //_MainPanel = SScrollContainer.Background(MainBg).Material(GameResources.GetMaterial)("DarkGreyBackGround")) SUI.
        // SContainerOptions scontainerOptions = SUI.SUI.RegisterNewPanel("ModSettingsPanel").OverrideSorting(200).Background(ModSettingsUi.MainBg)
        // .Material(GameResources.GetMaterial("DarkGreyBackground")).Dock(EDockType.Fill).Size(new float?(-400f), new float?(-100f));
        //var _panel = RegisterNewPanel(PANEL_NAME);
        // Ui creation code goes here
    }

}